const header=document.querySelector(".site-header"),menuToggle=document.querySelector(".menu-toggle"),primaryNav=document.querySelector(".primary-nav");window.addEventListener("scroll",()=>header&&header.classList.toggle("scrolled",window.scrollY>20));if(menuToggle&&primaryNav){menuToggle.addEventListener("click",()=>{const open=menuToggle.getAttribute("aria-expanded")==="true";menuToggle.setAttribute("aria-expanded",String(!open));menuToggle.setAttribute("aria-label",open?"Open navigation":"Close navigation");primaryNav.classList.toggle("open")});primaryNav.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>{primaryNav.classList.remove("open");menuToggle.setAttribute("aria-expanded","false")}))}const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("visible");observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll(".reveal").forEach(item=>observer.observe(item));const processTrack=document.querySelector(".process-track");if(processTrack){new IntersectionObserver(e=>{if(e[0].isIntersecting)processTrack.classList.add("visible")},{threshold:.35}).observe(processTrack)}const journeySteps=[...document.querySelectorAll(".journey-step")];let step=0;if(journeySteps.length)setInterval(()=>{journeySteps.forEach(x=>x.classList.remove("active"));journeySteps[step].classList.add("active");step=(step+1)%journeySteps.length},2000);const counter=document.querySelector(".counter");if(counter){new IntersectionObserver(entries=>{if(!entries[0].isIntersecting)return;let c=0,t=Number(counter.dataset.target),timer=setInterval(()=>{c+=1.7;if(c>=t){c=t;clearInterval(timer)}counter.textContent=c.toFixed(1)},25)},{threshold:.7}).observe(counter)}document.querySelectorAll("#year").forEach(el=>el.textContent=new Date().getFullYear());const form=document.querySelector("#contactForm");if(form)form.addEventListener("submit",e=>{e.preventDefault();const data=new FormData(form);const subject=encodeURIComponent(`Marval Parking Solutions Inquiry - ${data.get("interest")}`);const body=encodeURIComponent(`Name: ${data.get("name")}\nCompany: ${data.get("company")||""}\nEmail: ${data.get("email")}\nPhone: ${data.get("phone")||""}\nInterest: ${data.get("interest")}\n\nMessage:\n${data.get("message")}`);window.location.href=`mailto:office@marvalparkingsolutions.com?subject=${subject}&body=${body}`});